import { Inter, Istok_Web, Montserrat, Mulish, Orbitron } from 'next/font/google';

export const orbitron = Orbitron({ subsets: ["latin"], weight: '400' });
export const inter = Inter({ subsets: ['latin']});
export const montserrat = Montserrat({subsets: ['latin'], weight:'400'});
export const mulishBold = Mulish({subsets: ['latin'], weight:'700'});
export const istokWeb = Istok_Web({subsets: ['latin'], weight:'400'});